1. `False` > WRONG because `math.exp(1)` is e to the power of 1 which is e itself, so `True`.
2. the values generated by the `random` module will always be the same, unless `seed` is changed everytime.
3. `processor()`
4. `3`, because the output of `python_version_tuple()`is 3 element tuple.