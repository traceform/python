1. `3`
2. `6`
3. `tup.count(2)`
4.
```python
for key, value in item.items():
        d3.update({key: value})
``` 
> could be better

5. `tuple(my_list)`
6. `colors_dictionary = dict(colors)`
7. `{"A": 1, "B": 2}` because the `.copy()` method creates another dictionary with the same values instead of just referencing them unlike `copy_my_dictionary = my_dictionary`.
8. 
```
white : (255, 255, 255)
grey : (128, 128, 128)
red : (255, 0, 0)
green : (0, 128, 0)
```